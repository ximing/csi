{
  "name": "csi-extension",
  "private": true,
  "version": "0.8.0",
  "type": "module",
  "description": "CSI Chrome MV3 extension — lets a local daemon drive the browser over CDP",
  "scripts": {
    "build": "vite build",
    "watch": "vite build --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/node": "^26.1.2",
    "@vitest/coverage-v8": "^4.1.11",
    "jsdom": "^29.1.1",
    "typescript": "^7.0.2",
    "vite": "^6.0.0",
    "vitest": "^4.1.11"
  }
}
