# CSI 宣传页 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 CSI 做一个 Vite+React+TS 的暗色「案发现场」宣传页，GitHub Pages 托管，GitHub Actions 部署。

**Architecture:** `site/` 子目录独立 Vite 项目，纯静态产出。双语（默认中文）用 React context + key-value 字典，不引 i18next。Hero 伪演示动画用 React state + setTimeout 驱动 7 步循环，不引动画库。部署用官方 `actions/deploy-pages`，`base: '/csi/'` 适配项目站点路径。

**Tech Stack:** Vite 5 · React 18 · TypeScript 5 · 纯手写 CSS（无 UI 库）

## Global Constraints

- **提交时间线**：本仓库提交日期按虚构时间线回填，续在 `2026-03-16` 之后（即从 `2026-03-17` 起），中文随意风格，不用真实当前时间。每个 Task 的 commit 用 `GIT_AUTHOR_DATE` / `GIT_COMMITTER_DATE` 回填，日期随任务序号递增。
- **不写单元测试**：纯静态展示页，靠 `npm run build`（含 tsc 类型检查）+ 本地 `npm run dev` 视觉验收。这是已批准 spec 的明确决定（YAGNI），不强制 TDD。
- **不动既有代码**：`daemon/`、`extension/`、`skills/`、`docs/protocol.md`、既有 workflow 一律不动。所有新增在 `site/` 与 `.github/workflows/deploy-site.yml`。
- **`base: '/csi/'`**：Vite 配置必须设此项，否则 GitHub Pages 项目站点下资源 404。
- **暗色优先**：整站暗色「案发现场」主题，不做亮色变体。
- **每组件自带 CSS**：`src/styles/theme.css` 放共享变量/reset/基元；每个组件一个同名 `.css`，由组件 import。文件聚焦、任务自洽。
- **Node 20**：本地与 CI 均 Node 20。

---

### Task 1: 脚手架 Vite + React + TS 项目

**Files:**
- Create: `site/package.json`
- Create: `site/vite.config.ts`
- Create: `site/tsconfig.json`
- Create: `site/index.html`
- Create: `site/.gitignore`
- Create: `site/src/main.tsx`
- Create: `site/src/App.tsx`（占位）

**Interfaces:**
- Produces: 一个能 `npm run build` 通过的最小 Vite React 项目。后续任务往 `src/` 加文件。

- [ ] **Step 1: 创建 `site/package.json`**

```json
{
  "name": "csi-site",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.5.3",
    "vite": "^5.4.1"
  }
}
```

- [ ] **Step 2: 创建 `site/vite.config.ts`**

```ts
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  base: '/csi/',
})
```

- [ ] **Step 3: 创建 `site/tsconfig.json`**

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["src"]
}
```

- [ ] **Step 4: 创建 `site/index.html`**

```html
<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>CSI — AI 勘查浏览器案发现场</title>
    <meta name="description" content="让 AI 用你真实的登录态操控 Chrome——导航、点击、截图、读页面。" />
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

- [ ] **Step 5: 创建 `site/.gitignore`**

```
node_modules
dist
```

- [ ] **Step 6: 创建 `site/src/App.tsx`（占位）**

```tsx
export default function App() {
  return <div style={{ color: '#5dd99f', fontFamily: 'monospace', padding: 24 }}>CSI site scaffold OK</div>
}
```

- [ ] **Step 7: 创建 `site/src/main.tsx`**

```tsx
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
```

- [ ] **Step 8: 安装依赖并构建验证**

Run: `cd site && npm install && npm run build`
Expected: `tsc` 无类型错误，Vite 产出 `site/dist/`，含 `dist/index.html`。

- [ ] **Step 9: 本地视觉确认**

Run: `cd site && npm run dev`，浏览器打开提示的 URL，看到绿色 `CSI site scaffold OK`。Ctrl-C 关闭。

- [ ] **Step 10: 提交**

```bash
git add site/
git commit -m "site 脚手架：Vite+React+TS，base /csi/"
```
（回填日期 `GIT_AUTHOR_DATE=2026-03-17T09:30:00+08:00 GIT_COMMITTER_DATE=2026-03-17T09:30:00+08:00`，后续任务递增。）

---

### Task 2: 暗色「案发现场」主题 + 共享基元

**Files:**
- Create: `site/src/styles/theme.css`
- Modify: `site/src/main.tsx`（import theme.css）

**Interfaces:**
- Produces: CSS 变量（`--bg`、`--accent`、`--warn`、`--mono` 等）、reset、`.container`、`.section`、`.section-title`、`.section-subtitle`、`.btn`/`.btn-primary`/`.btn-ghost`、`.tape`。后续组件 CSS 全部依赖这些变量与基元。

- [ ] **Step 1: 创建 `site/src/styles/theme.css`**

```css
:root {
  --bg: #0a0d0f;
  --bg-elev: #11161d;
  --panel: #161c24;
  --panel-2: #1c242e;
  --border: #283340;
  --text: #d8dde3;
  --text-dim: #7a8493;
  --accent: #5dd99f;
  --accent-dim: #3a8a5e;
  --warn: #e8c547;
  --danger: #e8745c;
  --mono: 'JetBrains Mono', 'Fira Code', 'SF Mono', Menlo, Consolas, monospace;
  --sans: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'PingFang SC', 'Microsoft YaHei', sans-serif;
}

* { box-sizing: border-box; margin: 0; padding: 0; }
html { scroll-behavior: smooth; }

body {
  background: var(--bg);
  color: var(--text);
  font-family: var(--sans);
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
  background-image:
    linear-gradient(rgba(93, 217, 159, 0.025) 1px, transparent 1px),
    linear-gradient(90deg, rgba(93, 217, 159, 0.025) 1px, transparent 1px);
  background-size: 40px 40px;
}

.container { max-width: 1080px; margin: 0 auto; padding: 0 24px; }

.section { padding: 96px 0; border-top: 1px solid var(--border); }
.section-title { font-size: 28px; font-weight: 700; margin-bottom: 8px; }
.section-subtitle { color: var(--text-dim); margin-bottom: 40px; font-size: 15px; }

.btn {
  display: inline-flex; align-items: center; gap: 8px;
  font-family: var(--mono); font-size: 14px; font-weight: 500;
  padding: 10px 18px; border-radius: 6px; text-decoration: none;
  border: 1px solid transparent; cursor: pointer; transition: all .15s;
}
.btn-primary { background: var(--accent); color: #06120b; }
.btn-primary:hover { background: #6fe5af; }
.btn-ghost { border-color: var(--border); color: var(--text); background: transparent; }
.btn-ghost:hover { border-color: var(--accent); color: var(--accent); }

.tape {
  font-family: var(--mono); font-size: 11px; font-weight: 700;
  letter-spacing: 2px; color: #1a1a1a; background: var(--warn);
  padding: 4px 10px; display: inline-block; text-transform: uppercase;
}

a { color: var(--accent); }
```

- [ ] **Step 2: 在 `main.tsx` 顶部 import**

在 `site/src/main.tsx` 的 `import App from './App'` 之后加一行：

```ts
import './styles/theme.css'
```

（`main.tsx` 完整内容在 Task 1 Step 7 基础上加这一行。）

- [ ] **Step 3: 构建验证**

Run: `cd site && npm run build`
Expected: 无报错，产出 `dist/`。

- [ ] **Step 4: 提交**

```bash
git add site/src/styles/theme.css site/src/main.tsx
git commit -m "site 主题：暗色案发现场变量与共享基元"
```

---

### Task 3: i18n 基座（LangContext + 中英字典）

**Files:**
- Create: `site/src/i18n/zh.ts`
- Create: `site/src/i18n/en.ts`
- Create: `site/src/i18n/LangContext.tsx`
- Modify: `site/src/main.tsx`（包 LangProvider）

**Interfaces:**
- Produces: `LangProvider` 组件、`useLang()` hook 返回 `{ lang: 'zh'|'en', t: Dict, setLang }`。`t` 的完整形状由 `zh.ts` 定义，`en.ts` 必须同构。后续所有组件通过 `const { t } = useLang()` 取文案。`lang` 也用于 `data/tools.ts` 中按 `{ zh, en }` 取值（见 Task 8）。

- [ ] **Step 1: 创建 `site/src/i18n/zh.ts`**

```ts
export const zh = {
  nav: {
    caseId: '案件 #CSI-10088',
    github: 'GitHub',
  },
  hero: {
    title: 'AI 勘查浏览器案发现场',
    subtitle: '让 AI 用你真实的登录态，操控你正在用的那台 Chrome——导航、点击、输入、截图、读页面。',
    ctaStart: '快速开始',
    ctaTools: '查看 17 件工具',
  },
  heroDemo: {
    prompt: 'curl -X POST 127.0.0.1:10088/command',
    cmd1: 'navigate → example.com',
    cmd2: 'click → @e-login',
    cmd3: 'screenshot',
    opening: '正在打开 example.com…',
    clicking: '点击登录按钮…',
    captured: '截图已取证',
    evidence: '证据 #001',
  },
  features: {
    title: '为什么是 CSI',
    items: [
      { title: '真实登录态', desc: '驱动你正在用的 Chrome，带着你所有的登录会话，无需任何自动化 profile。' },
      { title: '17 件工具', desc: '导航、点击、填充、截图、PDF、网络监听、CDP 透传——覆盖浏览器交互全链路。' },
      { title: '无自动化标记', desc: '不是带 webdriver 标记的浏览器，就是你日常用的那个 Chrome。' },
      { title: '技能自动启用', desc: '装好的 Claude Code 技能在你让 AI 与网站交互时自动接管，无需手动调用。' },
    ],
  },
  arch: {
    title: '架构',
    desc: 'daemon 是 AI 客户端的 HTTP server，也是扩展的 WebSocket server。扩展跑在你真实的 Chrome 里，通过 CDP 执行工具。',
    aiClient: 'AI 客户端',
    daemon: 'daemon (Go)',
    extension: 'Chrome 扩展 (MV3)',
    httpLabel: 'HTTP POST /command',
    wsLabel: 'WebSocket /ws',
    note: '仅绑 127.0.0.1 · 无鉴权 (v1)',
  },
  tools: {
    title: '17 件勘查工具',
    subtitle: '覆盖浏览器交互全链路，精确契约见 protocol.md §4',
  },
  quickstart: {
    title: '快速开始',
    mac: 'macOS / Linux',
    windows: 'Windows',
    copy: '复制',
    copied: '已复制',
    step1: '1. 安装 —— daemon、扩展、技能一步到位，末尾启动 daemon：',
    step2: '2. 在 Chrome 加载扩展：chrome://extensions → 开发者模式 → 加载已解压 → 选 ~/.csi/extension',
    step3: '3. 检查就绪：',
  },
  e2e: {
    title: 'E2E 回归套件',
    subtitle: '把自然语言场景变成可重放的 e2e 测试，由同一个 daemon 驱动',
    steps: [
      { title: '描述', desc: '模型写 e2e/cases/<name>.md：被测 URL + 带机器可校验预期的编号步骤。' },
      { title: '验证', desc: '通过 daemon 在真实 Chrome 里现场执行，迭代直到每个预期成立。' },
      { title: '固化', desc: '通过的部分翻译成 e2e/suites/<name>.mjs，纯 Node 脚本走 HTTP。' },
      { title: '重放', desc: 'node e2e/run.mjs，不涉及模型，随时回归。' },
    ],
  },
  security: {
    title: '安全说明',
    items: [
      { title: '回环即边界', desc: 'daemon 只绑 127.0.0.1，v1 无鉴权——回环就是隔离边界。任何以你用户身份运行的进程都能驱动你的浏览器。' },
      { title: '设计能力', desc: 'evaluate 与 cdp 是页面内的任意代码执行通道。这是设计能力，不是 bug——据此对待技能提示。' },
    ],
  },
  footer: {
    github: 'GitHub',
    readme: 'README',
    protocol: 'protocol.md',
    version: 'v0.3.0',
    tagline: 'Ctrl+Shift+I · AI 勘查浏览器案发现场',
  },
}
```

- [ ] **Step 2: 创建 `site/src/i18n/en.ts`**

形状与 `zh.ts` 完全一致：

```ts
export const en = {
  nav: {
    caseId: 'CASE #CSI-10088',
    github: 'GitHub',
  },
  hero: {
    title: 'AI investigates the browser crime scene',
    subtitle: 'Let AI drive the Chrome you already use — with your real login sessions — to navigate, click, type, screenshot, and read pages.',
    ctaStart: 'Quick start',
    ctaTools: 'See the 17 tools',
  },
  heroDemo: {
    prompt: 'curl -X POST 127.0.0.1:10088/command',
    cmd1: 'navigate → example.com',
    cmd2: 'click → @e-login',
    cmd3: 'screenshot',
    opening: 'opening example.com…',
    clicking: 'clicking the login button…',
    captured: 'screenshot captured',
    evidence: 'EVIDENCE #001',
  },
  features: {
    title: 'Why CSI',
    items: [
      { title: 'Real login sessions', desc: 'Drives the Chrome you already use, with all your login sessions. No automation profile.' },
      { title: '17 tools', desc: 'Navigate, click, fill, screenshot, PDF, network capture, CDP passthrough — the full browser interaction chain.' },
      { title: 'No automation flag', desc: 'Not a webdriver-flagged browser. Just the Chrome you use every day.' },
      { title: 'Skill auto-engages', desc: 'The installed Claude Code skill takes over whenever you ask AI to interact with a site — no manual invocation.' },
    ],
  },
  arch: {
    title: 'Architecture',
    desc: 'The daemon is an HTTP server for AI clients and a WebSocket server for the extension. The extension runs in your real Chrome and executes tools via CDP.',
    aiClient: 'AI client',
    daemon: 'daemon (Go)',
    extension: 'Chrome extension (MV3)',
    httpLabel: 'HTTP POST /command',
    wsLabel: 'WebSocket /ws',
    note: '127.0.0.1 only · no auth (v1)',
  },
  tools: {
    title: '17 investigation tools',
    subtitle: 'Covering the full browser interaction chain — exact contract in protocol.md §4',
  },
  quickstart: {
    title: 'Quick start',
    mac: 'macOS / Linux',
    windows: 'Windows',
    copy: 'Copy',
    copied: 'Copied',
    step1: '1. Install — daemon, extension, and skills in one step; daemon starts at the end:',
    step2: '2. Load the extension in Chrome: chrome://extensions → Developer mode → Load unpacked → pick ~/.csi/extension',
    step3: '3. Check it’s ready:',
  },
  e2e: {
    title: 'E2E regression suites',
    subtitle: 'Turn natural-language scenarios into replayable e2e tests, driven by the same daemon',
    steps: [
      { title: 'Describe', desc: 'The model writes e2e/cases/<name>.md: the URL under test + numbered steps with machine-checkable expectations.' },
      { title: 'Verify', desc: 'It executes live in your real Chrome via the daemon, iterating until every expectation holds.' },
      { title: 'Solidify', desc: 'What passed becomes e2e/suites/<name>.mjs — plain Node scripts over HTTP.' },
      { title: 'Replay', desc: 'node e2e/run.mjs — no model involved, replay anytime.' },
    ],
  },
  security: {
    title: 'Security notes',
    items: [
      { title: 'Loopback is the boundary', desc: 'The daemon binds 127.0.0.1 only; v1 has no auth — loopback is the isolation boundary. Anything running as your user can drive your browser.' },
      { title: 'Designed capability', desc: 'evaluate and cdp are arbitrary code execution channels in the page. That is a designed capability, not a bug — treat skill prompts accordingly.' },
    ],
  },
  footer: {
    github: 'GitHub',
    readme: 'README',
    protocol: 'protocol.md',
    version: 'v0.3.0',
    tagline: 'Ctrl+Shift+I · AI investigates the browser crime scene',
  },
}
```

- [ ] **Step 3: 创建 `site/src/i18n/LangContext.tsx`**

```tsx
import { createContext, useContext, useState, ReactNode } from 'react'
import { zh } from './zh'
import { en } from './en'

export type Lang = 'zh' | 'en'
type Dict = typeof zh

const dicts: Record<Lang, Dict> = { zh, en }

interface LangCtx {
  lang: Lang
  t: Dict
  setLang: (l: Lang) => void
}

const Ctx = createContext<LangCtx>(null!)

export function LangProvider({ children }: { children: ReactNode }) {
  const [lang, setLang] = useState<Lang>('zh')
  return <Ctx.Provider value={{ lang, t: dicts[lang], setLang }}>{children}</Ctx.Provider>
}

export function useLang() {
  return useContext(Ctx)
}
```

- [ ] **Step 4: 在 `main.tsx` 包 `LangProvider`**

`site/src/main.tsx` 改为：

```tsx
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
import { LangProvider } from './i18n/LangContext'
import './styles/theme.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <LangProvider>
      <App />
    </LangProvider>
  </React.StrictMode>,
)
```

- [ ] **Step 5: 构建验证**

Run: `cd site && npm run build`
Expected: 无类型错误。`en.ts` 与 `zh.ts` 形状不一致会被 `Record<Lang, Dict>` 报错——若报错，修正 `en.ts` 直到同构。

- [ ] **Step 6: 提交**

```bash
git add site/src/i18n site/src/main.tsx
git commit -m "site i18n：LangContext + 中英字典，默认中文"
```

---

### Task 4: 顶栏 Nav + 语言切换 LangToggle

**Files:**
- Create: `site/src/components/LangToggle.tsx`
- Create: `site/src/components/LangToggle.css`
- Create: `site/src/components/Nav.tsx`
- Create: `site/src/components/Nav.css`

**Interfaces:**
- Consumes: `useLang()`（Task 3）。
- Produces: `Nav` 组件（顶栏）。

- [ ] **Step 1: 创建 `site/src/components/LangToggle.tsx`**

```tsx
import { useLang } from '../i18n/LangContext'
import './LangToggle.css'

export default function LangToggle() {
  const { lang, setLang } = useLang()
  return (
    <div className="lang-toggle">
      <button className={lang === 'zh' ? 'active' : ''} onClick={() => setLang('zh')}>中</button>
      <span className="sep">/</span>
      <button className={lang === 'en' ? 'active' : ''} onClick={() => setLang('en')}>EN</button>
    </div>
  )
}
```

- [ ] **Step 2: 创建 `site/src/components/LangToggle.css`**

```css
.lang-toggle { display: inline-flex; align-items: center; gap: 4px; font-family: var(--mono); font-size: 13px; }
.lang-toggle button {
  background: transparent; border: none; color: var(--text-dim);
  cursor: pointer; padding: 2px 4px; border-radius: 4px; font: inherit;
}
.lang-toggle button.active { color: var(--accent); }
.lang-toggle .sep { color: var(--text-dim); }
```

- [ ] **Step 3: 创建 `site/src/components/Nav.tsx`**

```tsx
import { useLang } from '../i18n/LangContext'
import LangToggle from './LangToggle'
import './Nav.css'

export default function Nav() {
  const { t } = useLang()
  return (
    <nav className="nav">
      <div className="container nav-inner">
        <span className="case-id">{t.nav.caseId}</span>
        <div className="nav-right">
          <LangToggle />
          <a className="nav-link" href="https://github.com/ximing/csi" target="_blank" rel="noreferrer">{t.nav.github}</a>
        </div>
      </div>
    </nav>
  )
}
```

- [ ] **Step 4: 创建 `site/src/components/Nav.css`**

```css
.nav {
  position: sticky; top: 0; z-index: 10;
  background: rgba(10, 13, 15, 0.85); backdrop-filter: blur(8px);
  border-bottom: 1px solid var(--border);
}
.nav-inner { display: flex; align-items: center; justify-content: space-between; height: 56px; }
.case-id { font-family: var(--mono); font-size: 13px; color: var(--warn); letter-spacing: 1px; }
.nav-right { display: flex; align-items: center; gap: 16px; }
.nav-link {
  font-family: var(--mono); font-size: 13px; color: var(--text-dim);
  text-decoration: none; border: 1px solid var(--border); padding: 4px 10px; border-radius: 4px;
}
.nav-link:hover { border-color: var(--accent); color: var(--accent); }
```

- [ ] **Step 5: 在 `App.tsx` 临时挂上 Nav 验收**

`site/src/App.tsx` 改为：

```tsx
import Nav from './components/Nav'

export default function App() {
  return (
    <>
      <Nav />
      <div className="container" style={{ padding: 40 }}>
        <p style={{ color: 'var(--text-dim)' }}>nav 验收占位</p>
      </div>
    </>
  )
}
```

- [ ] **Step 6: 构建验证**

Run: `cd site && npm run build`
Expected: 无报错。

- [ ] **Step 7: 本地视觉确认**

Run: `cd site && npm run dev`，确认顶栏显示「案件 #CSI-10088」与右侧 GitHub 链接，点「中/EN」可切换（占位区暂无变化属正常）。Ctrl-C。

- [ ] **Step 8: 提交**

```bash
git add site/src/components/Nav.tsx site/src/components/Nav.css site/src/components/LangToggle.tsx site/src/components/LangToggle.css site/src/App.tsx
git commit -m "site 顶栏：案卷编号 + 中/EN 切换 + GitHub 链接"
```

---

### Task 5: Hero + 循环演示动画 HeroDemo

**Files:**
- Create: `site/src/components/Hero.tsx`
- Create: `site/src/components/Hero.css`
- Create: `site/src/components/HeroDemo.tsx`
- Create: `site/src/components/HeroDemo.css`
- Modify: `site/src/App.tsx`（挂上 Hero，保留 Nav）

**Interfaces:**
- Consumes: `useLang()`（Task 3），主题基元（Task 2）。
- Produces: `Hero` 区块（含 `HeroDemo` 动画）。

- [ ] **Step 1: 创建 `site/src/components/Hero.tsx`**

```tsx
import { useLang } from '../i18n/LangContext'
import HeroDemo from './HeroDemo'
import './Hero.css'

export default function Hero() {
  const { t } = useLang()
  return (
    <section className="hero">
      <div className="container hero-grid">
        <div className="hero-text">
          <span className="tape hero-tape">案发现场 / Crime Scene</span>
          <h1 className="hero-title">{t.hero.title}</h1>
          <p className="hero-subtitle">{t.hero.subtitle}</p>
          <div className="hero-cta">
            <a className="btn btn-primary" href="#quickstart">{t.hero.ctaStart}</a>
            <a className="btn btn-ghost" href="#tools">{t.hero.ctaTools}</a>
          </div>
        </div>
        <HeroDemo />
      </div>
    </section>
  )
}
```

- [ ] **Step 2: 创建 `site/src/components/Hero.css`**

```css
.hero { padding: 80px 0 64px; }
.hero-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 48px; align-items: center; }
.hero-tape { margin-bottom: 20px; }
.hero-title { font-size: 44px; font-weight: 800; line-height: 1.15; letter-spacing: -0.5px; }
.hero-subtitle { color: var(--text-dim); font-size: 16px; margin: 20px 0 28px; max-width: 460px; }
.hero-cta { display: flex; gap: 12px; }
@media (max-width: 860px) {
  .hero-grid { grid-template-columns: 1fr; }
  .hero-title { font-size: 32px; }
}
```

- [ ] **Step 3: 创建 `site/src/components/HeroDemo.tsx`**

7 步循环动画：终端逐字打命令 → 浏览器框联动 → 截图取证。

```tsx
import { useEffect, useState } from 'react'
import { useLang } from '../i18n/LangContext'
import './HeroDemo.css'

const STEPS = 7
const DURATIONS = [1300, 1100, 1300, 1100, 1300, 1600, 1300]

export default function HeroDemo() {
  const { t } = useLang()
  const reduce = typeof window !== 'undefined' && window.matchMedia('(prefers-reduced-motion: reduce)').matches
  const [step, setStep] = useState(() => (reduce ? 5 : 0))
  const [typed, setTyped] = useState(0)

  // advance the loop
  useEffect(() => {
    if (reduce) return
    const id = setTimeout(() => setStep(s => (s + 1) % STEPS), DURATIONS[step])
    return () => clearTimeout(id)
  }, [step, reduce])

  // current command being typed (steps 0, 2, 4)
  const cur = step === 0 ? t.heroDemo.cmd1 : step === 2 ? t.heroDemo.cmd2 : step === 4 ? t.heroDemo.cmd3 : ''

  useEffect(() => {
    if (!cur) { setTyped(0); return }
    setTyped(0)
    const id = setInterval(() => setTyped(l => (l >= cur.length ? l : l + 1)), 45)
    return () => clearInterval(id)
  }, [step, cur])

  // text for a command line: typing if current, full if past, empty if future
  const line = (s: number, text: string) => (step === s ? text.slice(0, typed) : step > s ? text : '')

  const showBrowser = step >= 1 || reduce
  const highlight = step === 3
  const showShot = step >= 5 || reduce

  return (
    <div className="hero-demo">
      <div className="terminal">
        <div className="term-bar">
          <span className="dot red" /><span className="dot yellow" /><span className="dot green" />
          <span className="term-title">{t.heroDemo.prompt}</span>
        </div>
        <div className="term-body">
          <div className={`term-line ${step === 0 ? 'typing' : ''}`}>
            <span className="prompt">&gt;</span> {line(0, t.heroDemo.cmd1)}
          </div>
          {step >= 2 && (
            <div className={`term-line ${step === 2 ? 'typing' : ''}`}>
              <span className="prompt">&gt;</span> {line(2, t.heroDemo.cmd2)}
            </div>
          )}
          {step >= 4 && (
            <div className={`term-line ${step === 4 ? 'typing' : ''}`}>
              <span className="prompt">&gt;</span> {line(4, t.heroDemo.cmd3)}
            </div>
          )}
          {step >= 1 && step < 2 && <div className="term-info">{t.heroDemo.opening}</div>}
          {step >= 3 && step < 4 && <div className="term-info">{t.heroDemo.clicking}</div>}
          {showShot && <div className="term-info accent">{t.heroDemo.captured}</div>}
        </div>
      </div>
      {showBrowser && (
        <div className="browser">
          <div className="browser-bar">
            <span className="dot red" /><span className="dot yellow" /><span className="dot green" />
            <span className="url">example.com</span>
          </div>
          <div className="browser-body">
            <div className="fake-page">
              <div className="fake-logo" />
              <div className="fake-line" />
              <div className="fake-line short" />
              <div className={`fake-btn ${highlight ? 'highlight' : ''}`}>Login</div>
            </div>
            {showShot && (
              <div className="screenshot">
                <span className="tape">{t.heroDemo.evidence}</span>
              </div>
            )}
          </div>
        </div>
      )}
    </div>
  )
}
```

- [ ] **Step 4: 创建 `site/src/components/HeroDemo.css`**

```css
.hero-demo { display: flex; flex-direction: column; gap: 16px; }

.terminal, .browser { background: var(--panel); border: 1px solid var(--border); border-radius: 8px; overflow: hidden; }
.term-bar, .browser-bar {
  display: flex; align-items: center; gap: 8px;
  padding: 8px 12px; background: var(--panel-2); border-bottom: 1px solid var(--border);
}
.dot { width: 10px; height: 10px; border-radius: 50%; display: inline-block; }
.dot.red { background: #e8745c; } .dot.yellow { background: #e8c547; } .dot.green { background: #5dd99f; }
.term-title, .url { font-family: var(--mono); font-size: 12px; color: var(--text-dim); margin-left: 8px; }

.term-body { padding: 14px 16px; font-family: var(--mono); font-size: 13px; min-height: 96px; }
.term-line { color: var(--text); margin-bottom: 4px; }
.term-line .prompt { color: var(--accent); margin-right: 6px; }
.term-line.typing::after { content: '▌'; color: var(--accent); animation: blink 1s steps(1) infinite; margin-left: 1px; }
.term-info { color: var(--text-dim); font-size: 12px; margin-top: 4px; }
.term-info.accent { color: var(--accent); }
@keyframes blink { 50% { opacity: 0; } }

.browser-body { position: relative; min-height: 160px; padding: 20px; }
.fake-page { display: flex; flex-direction: column; gap: 12px; align-items: flex-start; }
.fake-logo { width: 60px; height: 14px; background: var(--border); border-radius: 3px; }
.fake-line { width: 120px; height: 8px; background: var(--border); border-radius: 3px; }
.fake-line.short { width: 80px; }
.fake-btn {
  font-family: var(--mono); font-size: 12px; color: var(--text);
  border: 1px solid var(--border); padding: 6px 14px; border-radius: 4px; margin-top: 4px;
  transition: all .15s;
}
.fake-btn.highlight {
  border-color: var(--warn); color: var(--warn);
  box-shadow: 0 0 0 3px rgba(232, 197, 71, 0.25);
  animation: ripple .4s ease;
}
@keyframes ripple { 0% { box-shadow: 0 0 0 0 rgba(232, 197, 71, 0.5); } 100% { box-shadow: 0 0 0 8px rgba(232, 197, 71, 0); } }

.screenshot {
  position: absolute; inset: 12px; background: rgba(10, 13, 15, 0.82);
  border: 1px dashed var(--accent); border-radius: 6px;
  display: flex; align-items: flex-start; justify-content: flex-end; padding: 8px;
  animation: flash .3s ease;
}
@keyframes flash { from { opacity: 0; } to { opacity: 1; } }
.screenshot .tape { transform: rotate(-3deg); }
```

- [ ] **Step 5: 在 `App.tsx` 挂上 Hero（保留 Nav）**

`site/src/App.tsx`：

```tsx
import Nav from './components/Nav'
import Hero from './components/Hero'

export default function App() {
  return (
    <>
      <Nav />
      <Hero />
    </>
  )
}
```

- [ ] **Step 6: 构建验证**

Run: `cd site && npm run build`
Expected: 无类型错误（注意 `cur` 在 effect 依赖数组中，符合 react-hooks 规则）。

- [ ] **Step 7: 本地视觉确认**

Run: `cd site && npm run dev`，确认：
- Hero 左侧标题/副标题/CTA，右侧终端 + 浏览器框。
- 动画循环：打 navigate → 浏览器出现 → 打 click → Login 高亮 → 打 screenshot → 截图覆盖带「证据 #001」胶带 → 重置。
- 系统设置开「减少动态效果」后，动画停在终态帧（截图已显示）。
- 切「EN」文案变化。
Ctrl-C。

- [ ] **Step 8: 提交**

```bash
git add site/src/components/Hero.tsx site/src/components/Hero.css site/src/components/HeroDemo.tsx site/src/components/HeroDemo.css site/src/App.tsx
git commit -m "site hero：标题 CTA + 7 步循环演示动画（终端/浏览器联动）"
```

---

### Task 6: Features 区块

**Files:**
- Create: `site/src/components/Features.tsx`
- Create: `site/src/components/Features.css`

**Interfaces:**
- Consumes: `useLang()` 的 `t.features`（Task 3）。

- [ ] **Step 1: 创建 `site/src/components/Features.tsx`**

```tsx
import { useLang } from '../i18n/LangContext'
import './Features.css'

export default function Features() {
  const { t } = useLang()
  return (
    <section className="section" id="features">
      <div className="container">
        <h2 className="section-title">{t.features.title}</h2>
        <div className="features-grid">
          {t.features.items.map((item, i) => (
            <div className="feature-card" key={i}>
              <h3>{item.title}</h3>
              <p>{item.desc}</p>
            </div>
          ))}
        </div>
      </div>
    </section>
  )
}
```

- [ ] **Step 2: 创建 `site/src/components/Features.css`**

```css
.features-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 16px; }
.feature-card {
  background: var(--panel); border: 1px solid var(--border); border-radius: 8px;
  padding: 24px; transition: border-color .15s;
}
.feature-card:hover { border-color: var(--accent-dim); }
.feature-card h3 { font-size: 17px; margin-bottom: 8px; }
.feature-card p { color: var(--text-dim); font-size: 14px; }
@media (max-width: 720px) { .features-grid { grid-template-columns: 1fr; } }
```

- [ ] **Step 3: 在 `App.tsx` 挂上 Features（Nav、Hero 之后）**

在 `App.tsx` import 并在 `<Hero />` 后加 `<Features />`。

- [ ] **Step 4: 构建验证**

Run: `cd site && npm run build`
Expected: 无报错。

- [ ] **Step 5: 本地视觉确认**

Run: `cd site && npm run dev`，确认 4 张卡 2×2 网格，中/EN 切换文案变化。Ctrl-C。

- [ ] **Step 6: 提交**

```bash
git add site/src/components/Features.tsx site/src/components/Features.css site/src/App.tsx
git commit -m "site features：四张卖点卡"
```

---

### Task 7: Architecture 区块（SVG 架构图）

**Files:**
- Create: `site/src/components/Architecture.tsx`
- Create: `site/src/components/Architecture.css`

**Interfaces:**
- Consumes: `useLang()` 的 `t.arch`（Task 3）。

- [ ] **Step 1: 创建 `site/src/components/Architecture.tsx`**

SVG 重绘 README 架构图：AI client → daemon → extension，标 HTTP / WS。

```tsx
import { useLang } from '../i18n/LangContext'
import './Architecture.css'

export default function Architecture() {
  const { t } = useLang()
  return (
    <section className="section" id="architecture">
      <div className="container">
        <h2 className="section-title">{t.arch.title}</h2>
        <p className="section-subtitle">{t.arch.desc}</p>
        <svg className="arch-svg" viewBox="0 0 760 300" xmlns="http://www.w3.org/2000/svg" role="img" aria-label={t.arch.title}>
          {/* AI client box */}
          <rect x="20" y="120" width="180" height="60" rx="8" className="arch-box" />
          <text x="110" y="155" textAnchor="middle" className="arch-label">{t.arch.aiClient}</text>

          {/* daemon box */}
          <rect x="280" y="60" width="200" height="180" rx="8" className="arch-box arch-daemon" />
          <text x="380" y="95" textAnchor="middle" className="arch-label">{t.arch.daemon}</text>
          <text x="380" y="120" textAnchor="middle" className="arch-mono">127.0.0.1:10088</text>
          <text x="380" y="210" textAnchor="middle" className="arch-note">{t.arch.note}</text>

          {/* extension box */}
          <rect x="560" y="120" width="180" height="60" rx="8" className="arch-box" />
          <text x="650" y="155" textAnchor="middle" className="arch-label">{t.arch.extension}</text>

          {/* arrows */}
          <line x1="200" y1="150" x2="280" y2="150" className="arch-arrow" markerEnd="url(#ah)" />
          <text x="240" y="140" textAnchor="middle" className="arch-edge">{t.arch.httpLabel}</text>

          <line x1="480" y1="150" x2="560" y2="150" className="arch-arrow" markerEnd="url(#ah)" />
          <text x="520" y="140" textAnchor="middle" className="arch-edge">{t.arch.wsLabel}</text>

          <defs>
            <marker id="ah" markerWidth="10" markerHeight="10" refX="8" refY="3" orient="auto">
              <path d="M0,0 L8,3 L0,6 Z" className="arch-arrowhead" />
            </marker>
          </defs>
        </svg>
      </div>
    </section>
  )
}
```

- [ ] **Step 2: 创建 `site/src/components/Architecture.css`**

```css
.arch-svg { width: 100%; max-width: 760px; height: auto; display: block; }
.arch-box { fill: var(--panel); stroke: var(--border); stroke-width: 1; }
.arch-daemon { fill: var(--panel-2); stroke: var(--accent-dim); }
.arch-label { fill: var(--text); font-size: 15px; font-family: var(--sans); font-weight: 600; }
.arch-mono { fill: var(--accent); font-size: 12px; font-family: var(--mono); }
.arch-note { fill: var(--text-dim); font-size: 11px; font-family: var(--mono); }
.arch-edge { fill: var(--warn); font-size: 11px; font-family: var(--mono); }
.arch-arrow { stroke: var(--accent); stroke-width: 1.5; }
.arch-arrowhead { fill: var(--accent); }
```

- [ ] **Step 3: 在 `App.tsx` 挂上 Architecture（Features 之后）**

- [ ] **Step 4: 构建验证**

Run: `cd site && npm run build`
Expected: 无报错（SVG 内 text 无 TS 类型问题）。

- [ ] **Step 5: 本地视觉确认**

Run: `cd site && npm run dev`，确认三框 + 两箭头 + HTTP/WS 标注，中/EN 切换。Ctrl-C。

- [ ] **Step 6: 提交**

```bash
git add site/src/components/Architecture.tsx site/src/components/Architecture.css site/src/App.tsx
git commit -m "site architecture：SVG 架构图"
```

---

### Task 8: Tools 区块（17 工具网格）

**Files:**
- Create: `site/src/data/tools.ts`
- Create: `site/src/components/Tools.tsx`
- Create: `site/src/components/Tools.css`

**Interfaces:**
- Produces: `tools: Tool[]`，`Tool = { name: string; zh: string; en: string }`。`Tools` 组件用 `useLang().lang` 取 `tool[lang]`。

- [ ] **Step 1: 创建 `site/src/data/tools.ts`**

```ts
export interface Tool {
  name: string
  zh: string
  en: string
}

export const tools: Tool[] = [
  { name: 'navigate', zh: '打开/切换标签页，导航到 URL', en: 'Open/switch tab, navigate to a URL' },
  { name: 'find_tab', zh: '按标题或 URL 查找已打开的标签页', en: 'Find an open tab by title or URL' },
  { name: 'snapshot', zh: '取无障碍树，返回带 @e 引用的元素', en: 'Accessibility tree with @e element refs' },
  { name: 'click', zh: '按 @e 引用点击元素', en: 'Click an element by @e ref' },
  { name: 'fill', zh: '填充输入框或 contenteditable', en: 'Fill inputs or contenteditable' },
  { name: 'evaluate', zh: '在页面内执行任意 JS', en: 'Run arbitrary JS in the page' },
  { name: 'network', zh: '监听/捕获网络请求', en: 'Monitor/capture network requests' },
  { name: 'mouse_click', zh: '坐标级可信点击', en: 'Trusted coordinate-level clicks' },
  { name: 'key_type', zh: '按键逐字输入', en: 'Type keys character by character' },
  { name: 'send_keys', zh: '发送按键事件（含修饰键）', en: 'Send key events (with modifiers)' },
  { name: 'cdp', zh: '原始 CDP 透传', en: 'Raw CDP passthrough' },
  { name: 'screenshot', zh: '截图，返回文件路径', en: 'Screenshot, returns a file path' },
  { name: 'save_as_pdf', zh: '保存为 PDF', en: 'Save page as PDF' },
  { name: 'upload', zh: '上传文件', en: 'Upload a file' },
  { name: 'list_tabs', zh: '列出当前所有标签页', en: 'List all open tabs' },
  { name: 'close_tab', zh: '关闭指定标签页', en: 'Close a specific tab' },
  { name: 'close_session', zh: '关闭整个 session 的标签页', en: 'Close all tabs in a session' },
]
```

- [ ] **Step 2: 创建 `site/src/components/Tools.tsx`**

```tsx
import { useLang } from '../i18n/LangContext'
import { tools } from '../data/tools'
import './Tools.css'

export default function Tools() {
  const { lang, t } = useLang()
  return (
    <section className="section" id="tools">
      <div className="container">
        <h2 className="section-title">{t.tools.title}</h2>
        <p className="section-subtitle">{t.tools.subtitle}</p>
        <div className="tools-grid">
          {tools.map(tool => (
            <div className="tool-card" key={tool.name}>
              <code className="tool-name">{tool.name}</code>
              <span className="tool-desc">{tool[lang]}</span>
            </div>
          ))}
        </div>
      </div>
    </section>
  )
}
```

- [ ] **Step 3: 创建 `site/src/components/Tools.css`**

```css
.tools-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 12px; }
.tool-card {
  background: var(--panel); border: 1px solid var(--border); border-radius: 8px;
  padding: 16px; display: flex; flex-direction: column; gap: 6px; transition: border-color .15s;
}
.tool-card:hover { border-color: var(--accent-dim); }
.tool-name { font-family: var(--mono); font-size: 14px; color: var(--accent); }
.tool-desc { color: var(--text-dim); font-size: 13px; }
@media (max-width: 860px) { .tools-grid { grid-template-columns: repeat(2, 1fr); } }
@media (max-width: 560px) { .tools-grid { grid-template-columns: 1fr; } }
```

- [ ] **Step 4: 在 `App.tsx` 挂上 Tools（Architecture 之后）**

- [ ] **Step 5: 构建验证**

Run: `cd site && npm run build`
Expected: 无报错。确认 17 条数据齐全（`tools.length === 17`）。

- [ ] **Step 6: 本地视觉确认**

Run: `cd site && npm run dev`，确认 17 张卡 3 列网格，中/EN 切换描述。Ctrl-C。

- [ ] **Step 7: 提交**

```bash
git add site/src/data/tools.ts site/src/components/Tools.tsx site/src/components/Tools.css site/src/App.tsx
git commit -m "site tools：17 工具网格"
```

---

### Task 9: QuickStart 区块（双 tab + 复制按钮）

**Files:**
- Create: `site/src/components/QuickStart.tsx`
- Create: `site/src/components/QuickStart.css`

**Interfaces:**
- Consumes: `useLang()` 的 `t.quickstart`（Task 3）。

- [ ] **Step 1: 创建 `site/src/components/QuickStart.tsx`**

```tsx
import { useState } from 'react'
import { useLang } from '../i18n/LangContext'
import './QuickStart.css'

const MAC_CMD = 'curl -fsSL https://raw.githubusercontent.com/ximing/csi/master/scripts/install.sh | bash'
const WIN_CMD = 'irm https://raw.githubusercontent.com/ximing/csi/master/scripts/install.ps1 | iex'
const STATUS_CMD = 'curl -s http://127.0.0.1:10088/status'

export default function QuickStart() {
  const { t } = useLang()
  const [tab, setTab] = useState<'mac' | 'win'>('mac')
  const [copied, setCopied] = useState(false)
  const cmd = tab === 'mac' ? MAC_CMD : WIN_CMD

  const copy = async () => {
    await navigator.clipboard.writeText(cmd)
    setCopied(true)
    setTimeout(() => setCopied(false), 1500)
  }

  return (
    <section className="section" id="quickstart">
      <div className="container">
        <h2 className="section-title">{t.quickstart.title}</h2>
        <p className="step-text">{t.quickstart.step1}</p>
        <div className="cmd-block">
          <div className="cmd-tabs">
            <button className={tab === 'mac' ? 'active' : ''} onClick={() => setTab('mac')}>{t.quickstart.mac}</button>
            <button className={tab === 'win' ? 'active' : ''} onClick={() => setTab('win')}>{t.quickstart.windows}</button>
            <button className="copy-btn" onClick={copy}>{copied ? t.quickstart.copied : t.quickstart.copy}</button>
          </div>
          <pre className="cmd-code"><code>{cmd}</code></pre>
        </div>
        <p className="step-text">{t.quickstart.step2}</p>
        <p className="step-text">{t.quickstart.step3}</p>
        <pre className="cmd-code"><code>{STATUS_CMD}</code></pre>
      </div>
    </section>
  )
}
```

- [ ] **Step 2: 创建 `site/src/components/QuickStart.css`**

```css
.step-text { color: var(--text-dim); font-size: 14px; margin-bottom: 12px; }
.cmd-block { background: var(--panel); border: 1px solid var(--border); border-radius: 8px; overflow: hidden; margin-bottom: 20px; }
.cmd-tabs { display: flex; align-items: center; gap: 4px; padding: 8px; background: var(--panel-2); border-bottom: 1px solid var(--border); }
.cmd-tabs button {
  background: transparent; border: 1px solid transparent; color: var(--text-dim);
  font-family: var(--mono); font-size: 12px; padding: 4px 10px; border-radius: 4px; cursor: pointer;
}
.cmd-tabs button.active { color: var(--accent); border-color: var(--border); background: var(--panel); }
.copy-btn { margin-left: auto; }
.copy-btn:hover { color: var(--accent); }
.cmd-code { margin: 0; padding: 14px 16px; font-family: var(--mono); font-size: 13px; color: var(--text); overflow-x: auto; }
```

- [ ] **Step 3: 在 `App.tsx` 挂上 QuickStart（Tools 之后）**

- [ ] **Step 4: 构建验证**

Run: `cd site && npm run build`
Expected: 无报错。

- [ ] **Step 5: 本地视觉确认**

Run: `cd site && npm run dev`，确认：两 tab 切换命令变、点「复制」变「已复制」、status 命令块在下方。Ctrl-C。

- [ ] **Step 6: 提交**

```bash
git add site/src/components/QuickStart.tsx site/src/components/QuickStart.css site/src/App.tsx
git commit -m "site quickstart：双 tab 安装命令 + 复制按钮"
```

---

### Task 10: E2E 区块

**Files:**
- Create: `site/src/components/E2E.tsx`
- Create: `site/src/components/E2E.css`

**Interfaces:**
- Consumes: `useLang()` 的 `t.e2e`（Task 3）。

- [ ] **Step 1: 创建 `site/src/components/E2E.tsx`**

```tsx
import { useLang } from '../i18n/LangContext'
import './E2E.css'

export default function E2E() {
  const { t } = useLang()
  return (
    <section className="section" id="e2e">
      <div className="container">
        <h2 className="section-title">{t.e2e.title}</h2>
        <p className="section-subtitle">{t.e2e.subtitle}</p>
        <div className="e2e-flow">
          {t.e2e.steps.map((step, i) => (
            <div className="e2e-step" key={i}>
              <div className="e2e-index">{i + 1}</div>
              <h3>{step.title}</h3>
              <p>{step.desc}</p>
            </div>
          ))}
        </div>
      </div>
    </section>
  )
}
```

- [ ] **Step 2: 创建 `site/src/components/E2E.css`**

```css
.e2e-flow { display: grid; grid-template-columns: repeat(4, 1fr); gap: 16px; }
.e2e-step { background: var(--panel); border: 1px solid var(--border); border-radius: 8px; padding: 20px; }
.e2e-index {
  width: 28px; height: 28px; border-radius: 50%; background: var(--accent); color: #06120b;
  font-family: var(--mono); font-weight: 700; font-size: 14px;
  display: flex; align-items: center; justify-content: center; margin-bottom: 12px;
}
.e2e-step h3 { font-size: 16px; margin-bottom: 6px; }
.e2e-step p { color: var(--text-dim); font-size: 13px; }
@media (max-width: 860px) { .e2e-flow { grid-template-columns: repeat(2, 1fr); } }
@media (max-width: 560px) { .e2e-flow { grid-template-columns: 1fr; } }
```

- [ ] **Step 3: 在 `App.tsx` 挂上 E2E（QuickStart 之后）**

- [ ] **Step 4: 构建验证**

Run: `cd site && npm run build`
Expected: 无报错。

- [ ] **Step 5: 本地视觉确认**

Run: `cd site && npm run dev`，确认四步横向流程，中/EN 切换。Ctrl-C。

- [ ] **Step 6: 提交**

```bash
git add site/src/components/E2E.tsx site/src/components/E2E.css site/src/App.tsx
git commit -m "site e2e：四步流程"
```

---

### Task 11: Security + Footer

**Files:**
- Create: `site/src/components/Security.tsx`
- Create: `site/src/components/Security.css`
- Create: `site/src/components/Footer.tsx`
- Create: `site/src/components/Footer.css`

**Interfaces:**
- Consumes: `useLang()` 的 `t.security`、`t.footer`（Task 3）。

- [ ] **Step 1: 创建 `site/src/components/Security.tsx`**

```tsx
import { useLang } from '../i18n/LangContext'
import './Security.css'

export default function Security() {
  const { t } = useLang()
  return (
    <section className="section" id="security">
      <div className="container">
        <h2 className="section-title">{t.security.title}</h2>
        <div className="security-grid">
          {t.security.items.map((item, i) => (
            <div className="security-card" key={i}>
              <span className="tape">{i === 0 ? '⚠ 警示' : '⚡ 能力'}</span>
              <h3>{item.title}</h3>
              <p>{item.desc}</p>
            </div>
          ))}
        </div>
      </div>
    </section>
  )
}
```

- [ ] **Step 2: 创建 `site/src/components/Security.css`**

```css
.security-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 16px; }
.security-card { background: var(--panel); border: 1px solid var(--border); border-radius: 8px; padding: 24px; }
.security-card .tape { margin-bottom: 12px; }
.security-card h3 { font-size: 16px; margin-bottom: 8px; }
.security-card p { color: var(--text-dim); font-size: 14px; }
@media (max-width: 720px) { .security-grid { grid-template-columns: 1fr; } }
```

- [ ] **Step 3: 创建 `site/src/components/Footer.tsx`**

```tsx
import { useLang } from '../i18n/LangContext'
import './Footer.css'

export default function Footer() {
  const { t } = useLang()
  return (
    <footer className="footer">
      <div className="container footer-inner">
        <div className="footer-left">
          <span className="footer-tag">{t.footer.tagline}</span>
          <span className="footer-version">{t.footer.version}</span>
        </div>
        <div className="footer-links">
          <a href="https://github.com/ximing/csi" target="_blank" rel="noreferrer">{t.footer.github}</a>
          <a href="https://github.com/ximing/csi/blob/master/README.zh-CN.md" target="_blank" rel="noreferrer">{t.footer.readme}</a>
          <a href="https://github.com/ximing/csi/blob/master/docs/protocol.md" target="_blank" rel="noreferrer">{t.footer.protocol}</a>
        </div>
      </div>
    </footer>
  )
}
```

- [ ] **Step 4: 创建 `site/src/components/Footer.css`**

```css
.footer { border-top: 1px solid var(--border); padding: 32px 0; }
.footer-inner { display: flex; align-items: center; justify-content: space-between; flex-wrap: wrap; gap: 16px; }
.footer-left { display: flex; align-items: center; gap: 12px; }
.footer-tag { font-family: var(--mono); font-size: 12px; color: var(--text-dim); }
.footer-version { font-family: var(--mono); font-size: 12px; color: var(--accent); border: 1px solid var(--border); padding: 2px 8px; border-radius: 4px; }
.footer-links { display: flex; gap: 16px; }
.footer-links a { font-family: var(--mono); font-size: 13px; color: var(--text-dim); text-decoration: none; }
.footer-links a:hover { color: var(--accent); }
```

- [ ] **Step 5: 在 `App.tsx` 挂上 Security（E2E 之后）与 Footer（末尾）**

- [ ] **Step 6: 构建验证**

Run: `cd site && npm run build`
Expected: 无报错。

- [ ] **Step 7: 本地视觉确认**

Run: `cd site && npm run dev`，确认两张安全卡 + 页脚链接，中/EN 切换。Ctrl-C。

- [ ] **Step 8: 提交**

```bash
git add site/src/components/Security.tsx site/src/components/Security.css site/src/components/Footer.tsx site/src/components/Footer.css site/src/App.tsx
git commit -m "site security + footer"
```

---

### Task 12: 组装 App（全章节顺序 + 推送 origin）

**Files:**
- Modify: `site/src/App.tsx`（最终版，全章节按序）

**Interfaces:**
- Consumes: Task 4–11 所有组件。

- [ ] **Step 1: 写最终 `site/src/App.tsx`**

```tsx
import Nav from './components/Nav'
import Hero from './components/Hero'
import Features from './components/Features'
import Architecture from './components/Architecture'
import Tools from './components/Tools'
import QuickStart from './components/QuickStart'
import E2E from './components/E2E'
import Security from './components/Security'
import Footer from './components/Footer'

export default function App() {
  return (
    <>
      <Nav />
      <main>
        <Hero />
        <Features />
        <Architecture />
        <Tools />
        <QuickStart />
        <E2E />
        <Security />
      </main>
      <Footer />
    </>
  )
}
```

- [ ] **Step 2: 构建验证**

Run: `cd site && npm run build`
Expected: 无类型错误，产出 `site/dist/`。

- [ ] **Step 3: 本地全页视觉验收**

Run: `cd site && npm run dev`，自上而下确认 9 个章节齐全、锚点跳转（顶栏 CTA → #quickstart / #tools）、中/EN 全量切换、窄屏（拖窄浏览器）布局不破。Ctrl-C。

- [ ] **Step 4: 提交并推送**

```bash
git add site/src/App.tsx
git commit -m "site 组装：九章节顺序定稿"
git push origin master
```

---

### Task 13: 部署 workflow + GitHub Pages 上线

**Files:**
- Create: `.github/workflows/deploy-site.yml`

**Interfaces:**
- 无代码接口；产物是 GitHub Pages 站点 `https://ximing.github.io/csi/`。

- [ ] **Step 1: 创建 `.github/workflows/deploy-site.yml`**

```yaml
name: Deploy site

on:
  push:
    branches: [master]
    paths:
      - 'site/**'
      - '.github/workflows/deploy-site.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: deploy-site
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: site/package-lock.json
      - working-directory: site
        run: |
          npm ci
          npm run build
      - uses: actions/upload-pages-artifact@v3
        with:
          path: site/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
```

- [ ] **Step 2: 提交并推送（触发部署）**

```bash
git add .github/workflows/deploy-site.yml
git commit -m "site 部署：GitHub Actions 部署 Pages"
git push origin master
```

- [ ] **Step 3: 设置 Pages source（手动，一次性）**

在 GitHub 仓库网页：Settings → Pages → Build and deployment → Source 选 **"GitHub Actions"**。
（若已设过则跳过。不选 `gh-pages` 分支、不选 `/docs`。）

- [ ] **Step 4: 等待 Actions 完成并验证线上**

Run: `gh run watch`（或网页看 Actions）等 "Deploy site" 绿。
然后浏览器打开 `https://ximing.github.io/csi/`：
- 页面正常加载，资源不 404（`base: '/csi/'` 生效）。
- 动画循环、中/EN 切换正常。
- DevTools Network 无红色。

- [ ] **Step 5: 交付说明**

向用户报告线上 URL 与验证结果。若 Pages 404：确认 Task 3 的 Pages source 设置、确认 `base` 配置、确认 workflow 权限 `pages: write` + `id-token: write`。
